{
  "$schema": "https://schemas.wp.org/trunk/wp-env.json",
  "plugins": [
    "."
  ],
  "lifecycleScripts": {
    "afterStart": "PLUGIN_SLUG=$(basename \"$PWD\"); for SERVICE in cli tests-cli; do wp-env run \"$SERVICE\" wp plugin activate \"$PLUGIN_SLUG\"; done"
  }
}
